{
  "name": "kns-calculator-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "kns-calculator monorepo — frontend (Next.js static на Yandex Object Storage) + backend (FastAPI на Yandex Cloud Functions + API Gateway)",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "next": "14.2.15"
  }
}
