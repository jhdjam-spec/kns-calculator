{
  "name": "kns-calculator-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel monorepo wrapper — frontend в frontend/, backend serverless в api/",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "next": "14.2.15"
  }
}
