# Зависимости для Yandex Cloud Functions runtime python311
# Цель: backend kns-calculator (FastAPI + Pydantic + ASGI-адаптер)

# Ядро API
fastapi>=0.110,<0.140
pydantic>=2.6,<3.0
starlette>=0.36
python-multipart>=0.0.9  # для /select/from-file FastAPI endpoint
slowapi>=0.1.9,<1.0  # rate-limit per IP (Sc.D. Architecture HIGH, 2026-05-13)

# YC Functions handler — кастомный YC event → ASGI scope (см. handler.py)
# Mangum НЕ работает: YC передаёт event в собственном формате,
# не совпадающем ни с одним AWS API Gateway вариантом.

# PDF/DOCX отчёты (RPZ ГОСТ Р 21.101, BOM ГОСТ 21.110, опросники)
reportlab>=4.0
python-docx>=1.1

# Хелперы для PDF (опросники с таблицами)
pdfplumber>=0.10

# S3 архив ТЗ (Yandex Object Storage). Lazy-import.
boto3>=1.34,<2.0
